---
import { getCollection } from "astro:content"
import BaseLayout from "@/layouts/BaseLayout.astro"

const allPosts = await getCollection("blog")
---

<BaseLayout title="News">
  <p>news list</p>
  <ul>
    {
      allPosts.map((post) => (
        <li>
          <li>
            <a href={`/n/${post.id}/`}>{post.data.title}</a>
          </li>
        </li>
      ))
    }
  </ul>
</BaseLayout>
